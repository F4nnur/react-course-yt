import React from 'react';
import preloader from "../../preloader.svg";

const Preloader = () => {
    return (
        <div>
            <img alt={'f'} src={preloader}/>
        </div>
    );
};

export default Preloader;